# PayRaizen API Configuration
# For local development with MAMP, you may need to disable SSL verification
# ONLY set this to 'false' in local development, NEVER in production!
# PAYRAIZEN_VERIFY_SSL=false

# Optional: Specify custom CA bundle path if needed
# CURL_CA_BUNDLE=/path/to/cacert.pem
